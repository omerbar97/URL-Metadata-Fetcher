{
  "name": "basic-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "cd frontend && npm i && cd ../backend && npm i",
    "build": "cd frontend && npm run build && cd ../backend && npm run build && cp -r ./public ./dist",
    "start": "cd backend && npm run start",
    "prod": "cd backend && npm run prod"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
  }
}
